
  # Recipes planner app

  This is a code bundle for Recipes planner app. The original project is available at https://www.figma.com/design/VXafWg3JZ60KJEcrrx2dEE/Recipes-planner-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  